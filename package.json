{
  "name": "oanda-api-ts",
  "version": "0.0.0",
  "description": "oanda-api-ts",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [
    "oanda-api-ts"
  ],
  "scripts": {
    "test": "mocha -r ts-node/register test/**/*.spec.ts",
    "lint": "gts lint",
    "check-src": "gts check src/**/*.ts",
    "check-test": "gts check test/**/*.ts",
    "check-all": "npm run check-src && npm run check-test",
    "clean": "gts clean",
    "compile": "tsc",
    "fix-src": "gts fix src/**/*.ts",
    "fix-test": "gts fix test/**/*.ts",
    "fix-all": "npm run fix-src && npm run fix-test",
    "prepare": "npm run compile",
    "pretest": "npm run fix-all && npm run compile"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.11.2",
    "chai": "^4.2.0",
    "gts": "^3.0.3",
    "mocha": "^8.2.1",
    "nyc": "^15.1.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "decimal.js": "^10.2.0",
    "json2typescript": "^1.4.1"
  }
}
